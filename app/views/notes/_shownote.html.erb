  <div class="shownote">
    <%= simple_format @note.body%>
  </div>




  <div class="actions">
    <!-- <% if @note.created_at > 5.minutes.ago %>
      <p><%= link_to "Edit this note", edit_note_path(@note), :class => "effect-shine"%></p>
      <p><%= link_to "Delete this note", note_path(@note), method: :delete, data: {confirm: "Are you sure??"}, :class => "effect-shine" %></p>
    <% end %> -->

    <% if session[:notes].include?(@note.id) %>
      <p><%= link_to "Edit this note", edit_note_path(@note), :class => "effect-shine"%></p>
      <p><%= link_to "Delete this note", note_path(@note), method: :delete, data: {confirm: "Are you sure??"}, :class => "effect-shine" %></p>
    <% end %>
  </div>


<footer>

  <p>Created <%= time_ago_in_words @note.created_at %> ago</p>


</footer>
